# kotlinTestAutomation
To set browser use the following params:

`-Dbrowser=FF|CHROME (CHROME by default)`

To use selenoid or other remote driver:

`-DisRemote=false|true`

`-DremoteWebdriverHost=127.0.0.1`